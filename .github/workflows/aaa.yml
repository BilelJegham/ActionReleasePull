name: Merge Translation

on:
  pull_request:

jobs:
  merge-me:
    if: ${{ github.event.pull_request.title == 'Update README' && github.actor == 'BilelJegham' }}
    name: Merge
    runs-on: ubuntu-latest
    steps:
      - uses: actions-ecosystem/action-add-labels@v1
        with:
          labels: translation
      - name: Merge me!
        uses: ridedott/merge-me-action@v1
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
